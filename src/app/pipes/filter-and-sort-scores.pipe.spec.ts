import { FilterAndSortScoresPipe } from './filter-and-sort-scores.pipe';

describe('FilterAndSortScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAndSortScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
