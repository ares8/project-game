import { FilterAndSortHistoryPipe } from './filter-and-sort-history.pipe';

describe('FilterAndSortHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAndSortHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
